import { styled } from "@mui/material";

export const UlWrapperFavorite = styled("ul")(() => {
  return {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    marginLeft: "0rem",
    gap: "unset",
    paddingLeft: 0,
    justifyContent: "space-around",
  };
});
