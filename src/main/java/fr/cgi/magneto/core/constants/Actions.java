package fr.cgi.magneto.core.constants;

public class Actions {
    // A user creates a board
    public static final String BOARD_CREATION = "BOARD_CREATION";

    // A user duplicate a board
    public static final String BOARD_DUPLICATE = "BOARD_DUPLICATE";

    // A user updates a board
    public static final String BOARD_UPDATE = "BOARD_UPDATE";

    // A user deletes a board
    public static final String BOARD_DELETION = "BOARD_DELETION";

    // A user creates a card
    public static final String CARD_CREATION = "CARD_CREATION";

    // A user updates a card
    public static final String CARD_UPDATE = "CARD_UPDATE";

    // A user deletes a card
    public static final String CARD_DELETION = "CARD_DELETION";
}
